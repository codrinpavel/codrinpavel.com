---
layout: base.njk
htmlClass: "projects"
title: "Projects"
order: 2
---

<section class="projects" id="projects">
  <div class="wrapper">
    <div class="columns md:columns-6 lg:columns-3 --row-gap-8xl --column-gap-8xl">
      {% for item in projects %}
        {%- set image_url = 'work/' ~ item.client | slugify ~ '-' ~ item.project | slugify ~ '-' ~ item.year ~ '.png' -%}
        {%- set permalink = "/projects/" ~ item.client | slugify ~ "-" ~ item.year ~ "/" -%}

        <figure class="select lg:ml-1 lg:mr-2">
          <a href="{{ permalink | url }}"></a>

          <div class="select__img">
            {% img image_url,
            "" %}
            {% img image_url,
            "" %}
          </div>

          <figcaption class="type-headline">
            <div>
              <span>Client</span>
              <span class="color-black-text">{{item.client}}</span>
            </div>
            <div>
              <span>Project</span>
              <span class="color-black-text">{{item.project}}</span>
            </div>
          </figcaption>
        </figure>

      {% endfor %}
    </div>
  </div>
</section>